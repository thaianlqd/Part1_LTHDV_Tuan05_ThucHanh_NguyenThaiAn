<%- include("../partials/header") %>

<div class="table-container">
  <h2>Danh sách Sản phẩm</h2>
  <a class="btn-add" href="/products/new">➕ Thêm mới</a>

  <table>
    <thead>
      <tr>
        <th>Tên</th>
        <th>Giá</th>
        <th>Số lượng</th>
        <th>Nhà cung cấp</th>
        <th>Hành động</th>
      </tr>
    </thead>
    <tbody>
      <% if (products.length === 0) { %>
        <tr><td colspan="5">Không có sản phẩm nào.</td></tr>
      <% } %>
      <% products.forEach(p => { %>
        <tr>
          <td><%= p.name %></td>
          <td><%= p.price != null ? p.price : "" %></td>
          <td><%= p.quantity != null ? p.quantity : "" %></td>
          <td><%= p.supplier ? p.supplier.name : "—" %></td>
          <td class="actions">
            <a class="btn-edit" href="/products/<%= p._id %>/edit">✏️ Sửa</a>
            <form action="/products/<%= p._id %>?_method=DELETE" method="POST" style="display:inline;">
              <button class="btn-delete" type="submit" data-confirm>🗑️ Xóa</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<style>
  .table-container {
    max-width: 900px;
    margin: 30px auto;
    padding: 20px;
    background: #fff;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
  }

  h2 {
    text-align: center;
    margin-bottom: 20px;
    color: #343a40;
  }

  .btn-add {
    display: inline-block;
    margin-bottom: 15px;
    padding: 8px 14px;
    background: #28a745;
    color: #fff;
    border-radius: 6px;
    text-decoration: none;
    font-weight: bold;
    transition: background 0.3s ease;
  }

  .btn-add:hover {
    background: #218838;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    text-align: center;
  }

  table th, table td {
    padding: 12px;
    border: 1px solid #dee2e6;
  }

  table th {
    background: #f8f9fa;
  }

  .actions {
    display: flex;
    justify-content: center;
    gap: 8px;
  }

  .btn-edit {
    background: #007bff;
    color: #fff;
    padding: 6px 10px;
    border-radius: 5px;
    text-decoration: none;
    font-size: 14px;
    transition: background 0.3s;
  }

  .btn-edit:hover {
    background: #0056b3;
  }

  .btn-delete {
    background: #dc3545;
    color: #fff;
    padding: 6px 10px;
    border-radius: 5px;
    border: none;
    cursor: pointer;
    font-size: 14px;
    transition: background 0.3s;
  }

  .btn-delete:hover {
    background: #c82333;
  }
</style>

<%- include("../partials/footer") %>
