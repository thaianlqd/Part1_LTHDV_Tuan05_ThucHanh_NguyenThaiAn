<%- include("../partials/header") %>

<div class="container my-4">
  <div class="card shadow">
    <div class="card-body">
      <h2 class="text-center mb-4 text-dark">Danh sách Nhà cung cấp</h2>

      <div class="mb-3 text-end">
        <a href="/suppliers/new" class="btn btn-primary">➕ Thêm mới</a>
      </div>

      <div class="table-responsive">
        <table class="table table-bordered table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Tên</th>
              <th>Địa chỉ</th>
              <th>Số điện thoại</th>
              <th>Hành động</th>
            </tr>
          </thead>
          <tbody>
            <% if (suppliers.length === 0) { %>
              <tr>
                <td colspan="4" class="text-center">Không có nhà cung cấp nào.</td>
              </tr>
            <% } %>
            <% suppliers.forEach(s => { %>
              <tr>
                <td><%= s.name %></td>
                <td><%= s.address || "—" %></td>
                <td><%= s.phone || "—" %></td>
                <td class="d-flex justify-content-center gap-2">
                  <a href="/suppliers/<%= s._id %>/edit" class="btn btn-primary btn-sm">✏️ Sửa</a>
                  <form action="/suppliers/<%= s._id %>?_method=DELETE" method="POST" class="d-inline">
                    <button type="submit" class="btn btn-danger btn-sm">🗑 Xóa</button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>

<%- include("../partials/footer") %>
