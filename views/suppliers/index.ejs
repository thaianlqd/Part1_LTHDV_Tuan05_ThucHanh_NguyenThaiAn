<%- include("../partials/header") %>

<style>
  body {
    font-family: Arial, sans-serif;
    margin: 20px;
  }

  h2 {
    text-align: center;
    margin-bottom: 20px;
  }

  a.button {
    display: inline-block;
    padding: 6px 12px;
    margin-bottom: 15px;
    background-color: #007bff;
    color: white;
    text-decoration: none;
    border-radius: 4px;
    font-size: 14px;
  }

  a.button:hover {
    background-color: #0056b3;
  }

  table {
    width: 100%;
    border-collapse: collapse;
    margin: 0 auto;
  }

  th, td {
    border: 1px solid #ddd;
    padding: 8px 12px;
    text-align: left;
  }

  th {
    background-color: #f4f4f4;
    font-weight: bold;
  }

  tr:nth-child(even) {
    background-color: #fafafa;
  }

  td form {
    display: inline;
  }

  td button {
    background-color: #dc3545;
    color: white;
    border: none;
    padding: 5px 10px;
    border-radius: 4px;
    cursor: pointer;
  }

  td button:hover {
    background-color: #a71d2a;
  }

  td a {
    margin-right: 8px;
    color: #007bff;
    text-decoration: none;
  }

  td a:hover {
    text-decoration: underline;
  }

  footer {
    text-align: center;
    margin-top: 30px;
    padding: 10px;
    border-top: 1px solid #ddd;
    font-size: 14px;
    color: #555;
  }
</style>

<h2>Danh sách Nhà cung cấp</h2>
<a href="/suppliers/new" class="button">➕ Thêm mới</a>

<table>
  <thead>
    <tr>
      <th>Tên</th>
      <th>Địa chỉ</th>
      <th>Số điện thoại</th>
      <th>Hành động</th>
    </tr>
  </thead>
  <tbody>
    <% if (suppliers.length === 0) { %>
      <tr><td colspan="4" style="text-align:center;">Không có nhà cung cấp nào.</td></tr>
    <% } %>
    <% suppliers.forEach(s => { %>
      <tr>
        <td><%= s.name %></td>
        <td><%= s.address || "—" %></td>
        <td><%= s.phone || "—" %></td>
        <td>
          <a href="/suppliers/<%= s._id %>/edit">✏️ Sửa</a>
          <form action="/suppliers/<%= s._id %>?_method=DELETE" method="POST" style="display:inline;">
            <button type="submit">🗑 Xóa</button>
          </form>
        </td>
      </tr>
    <% }) %>
  </tbody>
</table>

<%- include("../partials/footer") %>
